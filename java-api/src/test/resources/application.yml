server:
  port: 0

agent:
  base-url: http://localhost:9999   # unused in controller tests (AgentGatewayService is mocked)

security:
  jwt:
    secret: test-secret-key-for-unit-tests-32chars!!
    expiration-ms: 3600000

spring:
  main:
    web-application-type: reactive
  jackson:
    serialization:
      indent-output: true
